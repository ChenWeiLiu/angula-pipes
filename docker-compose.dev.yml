version: "3.5"

services:
  angular-pipes:
    ports:
      - 4300:4200
    build:
      context: ./
      dockerfile: Dockerfile.dev
    container_name: "angular-typing"
    volumes:
      - ./:/usr/src/app/

